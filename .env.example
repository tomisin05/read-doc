# ============================================================
# VERBATIM EXTRACTOR — Environment Variables
# ============================================================
# Copy this to .env.local in frontend/ for local dev
# Add all VITE_* vars to Vercel project settings (Frontend)
# Add FIREBASE_SERVICE_ACCOUNT to Vercel (Backend)
# ============================================================

# ── Firebase Client (Frontend) ───────────────────────────────
# Get from: Firebase Console → Project Settings → Your Apps → Web app
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abcdef

# ── Backend API URL (Frontend) ───────────────────────────────
# In production this is just empty (same domain via vercel.json routing)
# For local dev, point to your local FastAPI server
VITE_API_URL=

# ── Firebase Admin SDK (Backend / Vercel env var) ───────────
# Get from: Firebase Console → Project Settings → Service Accounts → Generate new private key
# Paste the ENTIRE JSON content as a single-line string
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}
